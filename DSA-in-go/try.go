package main

import (
	"fmt"
)

type void struct{}

func main() {

	
/*
	set := map[int]void{}
	var nothing void
	set[1] = nothing
	set[5] = nothing
	set[0] = nothing
	set[-8] = nothing

	fmt.Println(set)
	keys := []int{}
	for key, val := range set {
		fmt.Println(key, val)
		keys = append(keys, key)
	}

	// sort by key
	sort.Ints(keys)
	fmt.Println(keys)
	
	// sort by val of map

	m := map[string] int {}
	m["abhisheak"] = 1
	m["golu"] = 20
	m["bholu"]= -8
	m["dimple"] = 0

	keyList := []string{}
	for k := range m {
		keyList = append(keyList, k)
	}

	// sort keyList by map value
	// sort.Strings(keyList, func (str1, str2 string) bool {
	// 	return str1 < str2
	// })
	fmt.Println()
	sort.SliceStable(keyList, func(i, j int) bool {
		return i < j
	})
	fmt.Println(keyList)

*/	 

}